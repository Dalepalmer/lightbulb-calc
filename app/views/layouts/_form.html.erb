<h4>Energy Savings Calculator</h4>
      <div class="row">
        <div class="col m12">
          <h5>Calculate how much money and energy you could potentially save by switching to LED lighting today</h5>
          <div class="col m12">
            <form id="lightbulb-form">
              <div class="input-field">
                <div class="col m12">
                <input type="text" id="bulb-count" required>
                <label for="bulb-count">Number of Bulbs you are willing to change in your household</label>
              </div>
              </div>
              <div class="input-field">
                <div class="col m12 input-field">
                  <input type="text" id="hours" required>
                  <label for="hours">Average number of hours used per day</label>
               </div>
              </div>
              <div class="input-field">
                <select id="current-type">
                  <option value="Incandescent">Incandescent</option>
                  <option value="CFL">Compact Fluorescent</option>
                </select>
                <label for="current-type">Current Bulb Type?</label>
              </div>
              <div class="input-field">
                <select id="new-type">
                  <option value="LED">LED</option>
                  <option value="CFL">Compact Fluorescent</option>
                </select>
                <label for="bulb-type-after">New Bulb Type?</label>
              </div>
              <div class="input-field">
                <button type="submit" class="btn">Calculate Your Savings</button>
              </div>
            </form>
          </div>
        </div>
      </div>
    <div id="output">
      <ul id="lightbulb-output"></ul>
      <ul id="lightbulb-year"></ul>
      <ul id="lightbulb-savings"></ul>
    </div>
   </div>
